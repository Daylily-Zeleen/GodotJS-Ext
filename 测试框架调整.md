# 测试框架调整方案

## 背景

### 原版 GodotJS 测试架构

GodotJS 原版作为 Godot 引擎模块编译，测试框架依赖：

- **测试头文件**：`tests/test_macros.h`（Godot 引擎内置）
- **测试宏**：`TEST_CASE`、`CHECK`、`MESSAGE`、`REQUIRE`
- **测试项目路径**：硬编码为 `"modules/" JSB_MODULE_NAME_STRING "/tests/project"`
- **触发方式**：通过 `dev_build=yes` 编译时，引擎自动运行所有注册的测试用例

### 当前 GodotJS-Ext 架构

已改造为独立 GDExtension，不再是 Godot 源码的一部分：

- 无法使用 `tests/test_macros.h`（引擎内部文件）
- 测试项目路径需要调整
- 需要独立的测试框架

### 可用资源

- `third/doctest/doctest.h` — doctest 单头文件测试框架（Godot 引擎本身也使用）
- `GodotJS/tests/*.h` — 现有测试用例代码

---

## 调整方案

### 1. 添加测试控制宏

**文件：** `GodotJS/jsb.config.h`

**修改内容：** 在文件末尾添加：

```cpp
#ifndef JSB_TESTS_ENABLED
#   if defined(DEBUG_ENABLED) || defined(TESTS_ENABLED)
#       define JSB_TESTS_ENABLED 1
#   else
#       define JSB_TESTS_ENABLED 0
#   endif
#endif
```

### 2. 替换测试头文件

**文件：** `GodotJS/tests/jsb_test_helpers.h`

**修改内容：** 将原有的：

```cpp
#include "tests/test_macros.h"
```

替换为：

```cpp
#define DOCTEST_CONFIG_NO_POSIX_SIGNALS
#define DOCTEST_CONFIG_SUPER_FAST_ASSERTS
#include "third/doctest/doctest.h"
```

**说明：** doctest 提供与 Godot 引擎相同的 `TEST_CASE`、`CHECK`、`REQUIRE`、`MESSAGE` 宏，测试代码无需修改。

### 3. 创建 doctest 实现入口

**文件：** `GodotJS/tests/jsb_test_main.cpp`（新建）

**内容：**

```cpp
#define DOCTEST_CONFIG_IMPLEMENT
#include "third/doctest/doctest.h"

// 包含所有测试头文件以注册 TEST_CASE
#include "jsb_test_helpers.h"
#include "test_jsb_sarray.h"
#include "test_jsb_any_runtime.h"
#if JSB_WITH_QUICKJS
#include "test_jsb_quickjs_runtime.h"
#endif
#if JSB_WITH_V8
#include "test_jsb_v8_runtime.h"
#endif

// doctest 会自动收集所有 TEST_CASE 并在 main() 中运行
```

### 4. 修改构建脚本

**文件：** `GodotJS/SCsub`

#### 4.1 添加测试编译开关

在 `jsb_defines` 列表中添加：

```python
CompileDefines("JSB_TESTS_ENABLED", 1 if (env["dev_build"] or env.get("tests", False)) else 0),
```

#### 4.2 添加测试源文件

在 `module_obj` 相关代码附近添加：

```python
# 测试源文件（仅在 JSB_TESTS_ENABLED 时编译）
if env.get("tests", False) or env["dev_build"]:
    env_jsb.add_source_files(module_obj, "tests/jsb_test_main.cpp")
```

### 5. 修改 GDExtension 初始化

**文件：** `GodotJS/register_types.cpp`

在 `jsb_startup()` 函数末尾添加：

```cpp
#ifdef JSB_TESTS_ENABLED
    UtilityFunctions::print("Running GodotJS C++ unit tests...");
    doctest::Context ctx;
    ctx.setOption("abort-after", 0);    // 不在第一个失败后中止
    ctx.setOption("no-breaks", true);   // 不在断言失败时中断
    ctx.setOption("success", true);     // 显示成功通过的测试
    ctx.setOption("no-colors", true);   // CI 环境禁用颜色

    int test_result = ctx.run();

    if (test_result != 0) {
        UtilityFunctions::printerr("Doctest: ", test_result, " test case(s) failed");
    } else {
        UtilityFunctions::print("Doctest: All tests passed");
    }
#endif
```

### 6. 调整测试项目路径

**文件：** `GodotJS/tests/jsb_test_helpers.h`

调整 `GodotJSScriptLanguageIniter` 中的默认路径：

```cpp
GodotJSScriptLanguageIniter(const String p_base_path)
{
    // 如果 p_base_path 为空，使用相对路径
    String base_path = p_base_path;
    if (base_path.is_empty()) {
        // 适配 GDExtension 场景：测试项目位于 ./tests/project/
        base_path = "tests/project";
    }
    // ... 其余逻辑不变
}
```

---

## 使用方式

### 本地开发

```bash
# 启用测试（dev_build 已包含）
scons target=editor dev_build=yes -j10

# 或显式启用
scons target=editor tests=yes -j10
```

### CI 集成

测试通过 Godot 官方二进制运行测试项目（TypeScript 集成测试），C++ 单元测试在 GDExtension 加载时自动运行。

---

## 注意事项

1. **C++ 单元测试 vs TypeScript 集成测试**
   - C++ 单元测试：通过 doctest 在 GDExtension 加载时运行
   - TypeScript 集成测试：通过场景驱动，保持原有 sentinel 模式

2. **doctest 输出**
   - 默认输出到 stdout/stderr
   - 通过 `ctx.setOption("no-colors", true)` 适配 CI 环境

3. **测试项目迁移**
   - 根据 `文件迁移.md`，测试项目将从 `GodotJS/tests/project/` 迁移到 `./tests/project/`
   - 路径调整需在迁移完成后进行

---

## 验证步骤

1. 确认 `third/doctest/doctest.h` 存在且可用
2. 确认 `GodotJS/tests/*.h` 仍使用 `TEST_CASE`/`CHECK`/`MESSAGE`/`REQUIRE` 宏
3. 确认新增的 `jsb_test_main.cpp` 被编译进模块
4. 确认 `register_types.cpp` 中测试调用被 `JSB_TESTS_ENABLED` 条件编译保护
5. 运行 `scons target=editor dev_build=yes tests=yes` 验证编译通过
6. 运行 Godot 编辑器，检查控制台输出中的测试结果
